/**
 * Initialisation function here...
 */
async function init() {
    const go = new Go();
    // So here I will initialyse go wasm go to be run...
    /* 
	let result = await WebAssembly.instantiateStreaming(fetch("sql_client.wasm"), go.importObject)
    go.run(result.instance);
	*/

}

init();